﻿# ⚾ 中華職棒球票媒合與寄賣平台 - 系統需求規格書與開發指令

## 壹、 系統需求規格書 (v1.0)

### 一、 專案核心目標
打造專屬中職球迷的球票（及未來週邊商品）媒合平台，解決現有臉書社團資訊混亂、交易狀態不明的痛點。核心主打「快速雙向媒合看板」、「賣家自主挑選買家（確保讓票給同隊球迷）」以及「履約保障狀態機」。

### 二、 開發技術選型
*   **後端框架**：Python / Flask
*   **資料庫**：SQLite（開發與 MVP 階段）/ 搭配 Flask-SQLAlchemy (ORM)
*   **前端介面**：HTML, Bootstrap 5, Jinja2 模板引擎
*   **會話管理**：Flask-Login

### 三、 第一階段：MVP 最小可行性產品 (Phase 1)
本階段專注於打通「媒合」與「買賣流程」的核心動線。

**1. 會員系統 (基礎版)**
*   提供傳統的帳號、信箱、密碼註冊與登入。
*   **【重點】**個人資料設定中，**強制填寫「個人公開的 Facebook 或 Instagram 網址」**，作為初期供賣家審核身分的依據。

**2. 快速媒合雙看板 (核心 UI)**
*   首頁並排呈現「待售列表（Want to Sell）」與「徵票列表（Want to Buy）」。
*   提供快速標籤篩選功能（依照：球隊、球場、區域進行連動篩選）。

**3. 賣方寄賣與審核機制**
*   **上架**：賣家建立待售票券（欄位：球隊、球場、區域、票券型態、價格）。
*   **非先搶先贏**：買方點擊「申請購買」後，系統不直接結帳，而是將申請單送至賣家後台。
*   **賣家審核**：賣家可檢視所有申請人的名單與填寫的「社群連結」，自行判斷並勾選決定將票賣給哪一位球迷。

**4. 模擬履約保障機制 (Mocked Escrow)**
*   不串接真實金流，僅透過「按鈕」推進訂單狀態，確保交易動線完整。
*   **狀態機流程**：
    1.  買家點擊「模擬付款」 ➔ 狀態更新為「已付款，待給票」。
    2.  賣家點擊「已轉贈 / 已寄出」 ➔ 狀態更新為「已出貨，待確認」。
    3.  買家點擊「確認收票」 ➔ 狀態更新為「交易完成」（模擬平台撥款給賣家）。

---

### 四、 第二階段：進階安全防護與擴充 (Phase 2)
待第一階段核心功能開發完畢且運作順暢後，再行導入以下功能。

**1. 【防黃牛升級】強制社群連動登入 (OAuth 2.0)**
*   廢除傳統密碼註冊與手動填寫社群網址。全面改用 Facebook Login 或 Instagram Login 授權機制。
*   目的：系統直接抓取官方驗證的 User ID，大幅增加黃牛自動化註冊與使用假帳號的成本。

**2. 商品擴充彈性**
*   活化資料庫中的 `category` 欄位設計，將交易標的從「球票」無痛延伸至「扭蛋、球衣、實戰用品」等實體週邊商品。

**3. 自動配對通知系統**
*   當賣家上架的球票，剛好符合某位買家刊登的「徵票需求」時，系統自動發送 Email 或站內通知提醒買家。

---
---

## 貳、 給 Claude Code 的初始開發指令 (Prompt)

請直接複製以下 `【】` 內的完整文字貼給 Claude，讓它為您生成第一階段的基礎架構：

```text
你現在是一位資深的 Python Full-Stack 開發者。請幫我用 Flask 開發一個「中華職棒（CPBL）專屬媒合與寄賣網站」的 MVP (Phase 1)。

【專案背景與核心目標】
為解決臉書社團買賣票券資訊混亂的痛點，本系統主打「首頁雙向媒合看板」、「賣家自主挑選買家」及「模擬履約保障狀態機」。未來將擴充周邊商品交易，請在資料庫預留擴充彈性。

【技術選型】
- 後端：Flask, Flask-SQLAlchemy, Flask-Login
- 前端：HTML, Bootstrap 5, Jinja2
- 資料庫：SQLite

【核心功能需求】
1. 會員系統：帳號密碼註冊/登入，個人資料【必填】Facebook 或 Instagram 網址。
2. 首頁雙看板：並排呈現「待售列表(Want to Sell)」與「徵票列表(Want to Buy)」。具備球隊與球場的快速標籤篩選。
3. 寄賣與審核機制：
   - 賣家上架球票（球隊、球場、區域、票種、價格）。
   - 買方點擊「申請購買」後，賣家在後台檢視申請人的社群連結，自主「勾選決定」得標者。
4. 模擬履約保障 (Mocked Escrow)：
   - 訂單成立後，透過按鈕切換推進狀態：買家「模擬付款」 ➔ 賣家「已轉贈/已寄出」 ➔ 買家「確認收件」（訂單完成）。

【資料庫模型 (Models) 規劃與擴充性設計】
請在設計表結構時加入 `category` 欄位預留擴充。
- User: id, username, email, password_hash, social_link, rating_avg(快取平均評分，可由 Rating 表計算後更新), default_count(累計棄標/違約次數)
- Listing: id, seller_id, category(預設為'ticket'), team, stadium, zone, type, delivery_method, price, status
- WantToBuy: id, buyer_id, category, team, stadium, zone
- Application: id, listing_id, buyer_id, status, created_at
- Order: id, listing_id, buyer_id, seller_id, status(新增 buyer_defaulted=買家棄標未付款、seller_defaulted=賣家毀約未出貨 兩種違約狀態), created_at, updated_at
- Rating: id, order_id, rater_id(評分者), ratee_id(被評分者), score(1-5), is_default(這筆交易是否屬於棄標/不履約), comment, created_at

【請提供以下輸出】
1. 具備未來擴充彈性的完整 models.py。
2. app.py 中處理「提交申請」、「賣家選定買家」與「狀態機推進」的核心 API 路由。
3. 首頁 (index.html) 的版面規劃草碼，以雙看板設計呈現供需列表。
4. 專案資料夾結構樹狀圖及 requirements.txt。
```

---
---

## 參、資深工程師審查意見（開發前必讀，待補充事項）

以下項目在目前規格中未定義或有風險，建議在動工前先拍板，避免 Phase 1 做完才發現要推翻資料模型或重寫核心流程。

### 一、法規／合規風險（優先度最高）
*   **轉售定價未設限**：平台核心目的是「解決黃牛亂象」，但目前規格完全沒有限制票券的轉售價格。若賣家可自由訂價高於原價，本平台在事實上等同二級黃牛市場，可能牴觸台灣《文化創意產業發展法》第 10-1 條對於「以顯不相當之對價轉售表演活動票券」的規範。
    *   **建議**：Listing 需增加 `original_price`（原始票面價）欄位，並在商業邏輯（非僅前端提示）層級限制 `price <= original_price`（或主管機關認定的合理漲幅上限），超過即擋下上架。
*   **平台定位聲明**：需要在使用者條款中明確平台僅提供「媒合」而非「代購/代銷」，釐清法律責任歸屬（例如票券真偽、交易糾紛由買賣雙方自負，平台僅盡合理審核義務）。

### 二、模擬履約機制（Mocked Escrow）與真實金流的落差
*   規格說「不串接真實金流，僅透過按鈕推進狀態」，但買賣雙方的實際金錢很可能是透過真人私下轉帳完成。這代表「模擬付款」按鈕與「真的付款了沒」之間沒有任何綁定，賣家可能在買家還沒真的轉帳前就被按下「已轉贈」，或買家轉了錢賣家卻不出票。
    *   **建議**：MVP 階段至少要有「爭議申訴」出口（例如訂單狀態新增 `disputed`，並提供客服/管理員介入的最低限度流程），不能只有三個樂觀路徑（付款→出貨→完成）沒有例外分支。
    *   長期仍建議規劃真實金流串接（如綠界/藍新）的路徑，MVP 的資料表設計應預留 `transaction_id` / `payment_method` 欄位，避免將來遷移時要改表結構。

### 三、資料模型缺口
現有模型只列了核心欄位，實作時至少會缺以下內容，建議先補進規格：
*   **Listing**：缺 `quantity`（連坐張數）、`created_at`/`updated_at`、票券憑證圖片欄位（`ticket_image_url`，用於降低假票風險）。
*   **Application / Order / Listing 的 status 未列舉可能值**，前後端容易對不齊。建議明確定義：
    *   `Listing.status`: `active` / `pending`（已有得標者，等待完成）/ `sold` / `cancelled`
    *   `Application.status`: `pending` / `accepted` / `rejected`
    *   `Order.status`: `awaiting_payment` / `paid` / `shipped` / `completed` / `disputed` / `cancelled`
*   **Application 缺 `created_at`**，賣家後台需要依申請時間排序，且要有「逾時未處理」的提醒機制（否則買方會一直卡在 pending 狀態不知道結果）。
*   **缺 Message/Note 資料表**：買賣雙方成交後如何約定取票方式（面交地點、寄送地址）？至少需要一個最簡單的訂單留言欄位，否則核心動線會卡在「怎麼交付」這一步。

### 四、併發與競態處理
*   一張 Listing 若同時收到多個 Application，賣家勾選其中一位得標者後：
    *   其餘申請人的狀態要如何處理（自動 `rejected`）？是否要通知？
    *   Listing 本身要立刻轉為不可再申請的狀態，避免同一張票被「賣兩次」。
*   目前規格沒有定義以上收斂邏輯，實作時容易漏掉，建議在狀態機圖裡明確畫出這個分支。

### 五、信任與防詐機制
*   **社群連結僅供「填寫」，未做真實性驗證**：Phase 1 靠賣家人工審核社群連結，但使用者可以隨意填一個不屬於自己的網址。建議至少要求連結需為「公開可瀏覽」且賣家審核時看得到照片/貼文，作為軟性防呆；Phase 2 的 OAuth 才是真正解法，這點應在 Phase 1 的文案/UI 提示中就講清楚「僅供參考、非強制驗證」，避免使用者誤以為已被平台驗證身分。
*   **信譽/評價系統（已於資料模型補上）**：新增 `Rating` 資料表（每筆訂單完成或違約後，買賣雙方互評 1-5 分，並標記 `is_default`），`User` 增加 `rating_avg` 與 `default_count` 兩個快取欄位。
    *   **棄標/不履約需明確定義觸發時機**：例如買家超過 N 小時未按「模擬付款」視為 `buyer_defaulted`；賣家超過 N 小時未按「已轉贈/已寄出」視為 `seller_defaulted`。建議用一個排程工作（cron/APScheduler）掃描逾時訂單並自動標記，而非只能靠對方手動申訴，否則違約方永遠不會被記錄。
    *   **顯示時機**：賣家審核申請人列表時，除了社群連結，應同時顯示申請人的 `rating_avg` 與 `default_count`，讓「自主挑選買家」有實際依據，而不是純粹主觀判斷。
    *   **防刷分**：評分僅能在訂單進入 `completed` 或違約終態後才能提交，且一筆 Order 對應最多兩筆 Rating（買評賣、賣評買），避免重複洗分。
*   **缺乏檢舉機制**：假票、辱罵、疑似黃牛轉賣等情況，需要一個最低限度的檢舉入口（哪怕只是 email 給管理員）。

### 六、安全性（規格未提及，實作時容易被跳過）
*   **密碼儲存**：規格提到「帳號、信箱、密碼註冊」，需明確要求密碼以雜湊儲存（如 `werkzeug.security.generate_password_hash`），不得明文或可逆加密存放。
*   **CSRF 防護**：狀態機推進（模擬付款、確認收票等）都是會改變資料的 POST 操作，技術選型應加入 **Flask-WTF** 或等效的 CSRF token 機制。
*   **個資曝光範圍**：賣家後台可看到所有申請人的社群連結，這代表申請購買=公開個人社群給陌生人看，需要在使用者條款/隱私權政策中明確告知，並考慮是否要有申請人自行撤回申請的功能。

### 七、通知機制不應該等到 Phase 2
*   規格把「自動配對通知」歸類到 Phase 2，但「賣家選定買家後，買家需要被通知」是 Phase 1 核心動線的一部分，沒有通知的話買家只能自己重新整理頁面查看，體驗會嚴重打折。
    *   **建議**：Phase 1 至少要有站內通知（一個簡單的 Notification 表或未讀狀態即可），Email 通知才留到 Phase 2 亦可。

### 八、上線前維運注意事項
*   `Flask` 開發伺服器（`app.run()`）與 `debug=True` 不可用於正式環境，正式部署需搭配 production WSGI server（如 waitress/gunicorn）並關閉 debug。
*   SQLite 僅適合 MVP 驗證，若同時有多個賣家/買家併發寫入（尤其是「勾選得標者」這種需要鎖定的操作），建議及早規劃遷移到 PostgreSQL 的路徑，避免 SQLite 檔案鎖定造成的併發寫入問題。
*   `SECRET_KEY`、資料庫連線字串等機密設定需透過環境變數注入，不可寫死在程式碼中提交版控。

### 九、驗收標準未定義
*   建議在 Phase 1 結案前，明確列出「核心動線」的驗收案例（例如：一個使用者能否完整走完「上架→申請→審核→模擬付款→出貨→完成」全流程且資料庫狀態正確），作為 Rule 4（Goal-Driven Execution）所要求的「成功標準」，而不是只憑功能清單打勾。
